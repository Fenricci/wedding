import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import WeddingInviteSite from './WeddingInviteSite.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WeddingInviteSite />
  </React.StrictMode>
);
